<!doctype html>
<html>
  <head>
    <%= render :partial => 'layouts/head' %>
  </head>
  <body>
    <header class="exhibition">
      <%= yield :exhibition %>
      <%= render :partial => 'layouts/navigation' %>
    </header>
    <div class="content gridlayout exhibition">
      <%= yield %>
    </div>
    <footer>
      <%= render :partial => 'layouts/toolbar' %>
    </footer>
  </body>
</html>

<script type="text/javascript">
  <!--
  if (jQuery.browser.msie){
    document.getElementById('ie_fallback').innerHTML = '<div class="browserWarning">Currently you are using Internet Explorer. The site will be working properly for you. Users with an HTML5 compatible browser will get a better experience on this website. We as the .projectlodge would like to advize you to use a modern browser. As an open-source community we obviously would like to point you to the <a href="http://www.firefox.com/">Mozilla Firefox</a> and <a href="http://chrome.google.com/">Google Chrome</a> browsers.</div>';
  }
  //-->
</script>